framework:
    secret: "Three can keep a secret, if two of them are dead."

cmobi_msf:
    log_path: "%kernel.root_dir%/logs/cmobi_msfbundle.log"
    rpc_servers:
        default:
            queue: { name: 'default_queue' }
